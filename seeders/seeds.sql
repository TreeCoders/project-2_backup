USE project_db;

-- CREATE TABLE posts(
-- author varchar(30) not null,
-- title VARCHAR(60) not null,
--  message varchar(2000) not null,
--   createdAt TIME not null
--   );
--
INSERT INTO posts (author, title, message)
Values ("Me", "Some title", "Some Message Yo!");
